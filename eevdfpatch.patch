--- 0001-EEVDF.patch.orig	2023-08-30 21:22:30.766946524 +0900
+++ 0001-EEVDF.patch	2023-08-30 21:27:40.769479403 +0900
@@ -416,6 +416,8 @@
  	p->se.nr_migrations		= 0;
  	p->se.vruntime			= 0;
 +	p->se.vlag			= 0;
+	p->se.dur_avg			= 0;
+	p->se.prev_sleep_sum_runtime	= 0;
  	INIT_LIST_HEAD(&p->se.group_node);
  
  #ifdef CONFIG_FAIR_GROUP_SCHED
@@ -789,6 +791,8 @@
  	p->se.vruntime			= 0;
  	p->se.vlag			= 0;
 +	p->se.slice			= sysctl_sched_min_granularity;
+	p->se.dur_avg			= 0;
+	p->se.prev_sleep_sum_runtime	= 0;
  	INIT_LIST_HEAD(&p->se.group_node);
  
  #ifdef CONFIG_FAIR_GROUP_SCHED
@@ -1889,8 +1893,8 @@
 - *
 - * (default: 1 msec * (1 + ilog(ncpus)), units: nanoseconds)
 - */
--unsigned int sysctl_sched_wakeup_granularity			= 1000000UL;
--static unsigned int normalized_sysctl_sched_wakeup_granularity	= 1000000UL;
+-unsigned int sysctl_sched_wakeup_granularity			= 3000000UL;
+-static unsigned int normalized_sysctl_sched_wakeup_granularity	= 3000000UL;
 -
  const_debug unsigned int sysctl_sched_migration_cost	= 500000UL;
  
@@ -2590,6 +2594,8 @@
  	p->se.vlag			= 0;
 -	p->se.slice			= sysctl_sched_min_granularity;
 +	p->se.slice			= sysctl_sched_base_slice;
+	p->se.dur_avg			= 0;
+	p->se.prev_sleep_sum_runtime	= 0;
  	INIT_LIST_HEAD(&p->se.group_node);
  
  #ifdef CONFIG_FAIR_GROUP_SCHED
